Some solutions of [Project Euler](https://projecteuler.net)

The code is in java 15 with vavr and apache commons.

You can run a specific problem like this:
`./gradlew run --args=4`

or a range of problems like this:
`./gradlew run --args="1 6"`
